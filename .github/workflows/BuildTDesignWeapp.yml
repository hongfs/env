name: Build TDesign MiniProgram

on:
  push:
    branches:
      - main

jobs:
  build-tdesign:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    concurrency:
      group: ${{ github.ref }}
      cancel-in-progress: true

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          repository: Tencent/tdesign-miniprogram
          ref: 1.0.4

      - name: Build
        run: |
          cd $GITHUB_WORKSPACE
          npm i
          npm run build
      - uses: ncipollo/release-action@v1
        with:
          artifacts: miniprogram_dist/
          tag: TDesign-1.0.4
